import React from 'react';
import { render } from 'react-dom';
import BitcoinTracker from './components/BitcoinTracker';
import './main.sass';

render(<BitcoinTracker />, document.getElementById('root'));
